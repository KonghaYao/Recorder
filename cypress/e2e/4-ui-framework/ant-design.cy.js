/// <reference types="cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('https://ant-design.antgroup.com/index-cn');
    // Resize window to 1000 x 660
    cy.viewport(1000, 660);
  });
  it('', () => {});
});
